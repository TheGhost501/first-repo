function fish(skumriq, caca, palamudkg, saphridkg, shellskg) {
    let palamud = (skumriq * palamudkg) * 1.6;
    let saphrid = (caca * saphridkg) * 1.8;
    let shells = shellskg * 7.5;
    console.log((palamud + saphrid + shells).toFixed(2))
}
fish(6.90, 4.20, 1.5, 2.5, 1)